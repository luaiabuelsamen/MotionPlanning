# Motion Planner

A C++ project for motion planning.

## Building

1. Create a build directory: `mkdir src/build && cd src/build`
2. Run CMake: `cmake ../..`
3. Build: `make`

## Running

After building, run `./motion_planner`# MotionPlanning
